meta {
  name: Generar pedido
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/pedidos
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenComprador}}
}

body:json {
  {
    "id_direccion": {{idDireccion}}
  }
}

vars:pre-request {
  baseUrl: http://127.0.0.1:8080
  idDireccion: 
}
