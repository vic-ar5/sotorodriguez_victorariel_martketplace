meta {
  name: Cambiar estado de producto
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/admin/productos/{{idProducto}}/estado
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

body:json {
  {
    "estado": "inactivo"
  }
}
