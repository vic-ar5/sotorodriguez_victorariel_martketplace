meta {
  name: Cambiar estado de categoria
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/admin/categorias/{{idCategoria}}/estado
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

body:json {
  {
    "activo": false
  }
}
